# Cross-skill index — load this when writing back a finding, or before starting work in a domain that might depend on another

This is an index, not a rulebook: every row states a fact in one line and points at the ONE place
that actually explains it, exactly like this skill's main checklist does for mod-content
conventions. **Never copy a rule's explanation into this table** — if a row needs more than one
line, that content belongs in the owning file, not here.

## Why this exists

A 2026-08-02 audit of the whole skill tree found the same shape of gap repeatedly: a gotcha
discovered while working in skill A turned out to be just as relevant to skill B, but nothing in B
ever pointed to it — sometimes for months. The clearest example: `cube-chaos-rule-text` pointed back
to `cube-chaos-scripting` six separate times; `cube-chaos-scripting` had never once pointed forward
to `cube-chaos-rule-text`, despite `CLAUDE.md` explicitly pairing the two skills' own rules in the
same sentence. Each individual gap is a one-line fix; the actual problem was that nothing was
tracking them as a set, so the same asymmetry kept recurring skill after skill.

## How this stays current

- **Live, while working:** `CLAUDE.md`'s research protocol asks every write-back to also check "is
  this relevant to a different skill's domain," and if so, add a pointer there *and* a row here, in
  the same edit.
- **Periodic/batch:** `cube-chaos-doc-audit`'s `GAP` findings are exactly what this table tracks —
  every real doc-audit pass should reconcile its GAP findings against this file (new rows for newly
  found gaps, status updates for rows that got linked).
- **Link status** is one of: `needs link` (the fact exists in exactly one place, no pointer from the
  other relevant skill(s) yet), `linked` (a pointer now exists both ways, or one-way where only one
  direction makes sense), `resolved` (kept here only as a worked example/calibration, not an open
  item).

## Index

| Topic | Lives at | Also relevant to | Status |
|---|---|---|---|
| Faction numbers: `DoubleConstant 1` = player/allies, `2` = enemy, independent of caster | `cube-chaos-scripting/references/gotchas-grepped.md:43` | `cube-chaos-scenario-scripting` (`DATA:`/`DATARECT:` faction args), `cube-chaos-balancing` (curse `Value:` ranges keyed to which side is targeted) | needs link |
| Faction symmetry: every ability must work for whichever side owns it — `Forwards`/`Backwards` not `East`/`West`, and `PlacabilityOfPosition` vs `FactionOfThis`/`FactionOfCube Caster` not a hardcoded faction number or board edge | `cube-chaos-scripting/references/targeting-movement-board.md` ("Finding my own edge column", "Enemy/allied territory", "Hardcoding East/West"); mandate stated in `CLAUDE.md` | `cube-chaos-scenario-scripting` (owns `DATA:`/`DATARECT:`/`PLACERECT:` faction args — the scenario-level expression of the same board-ownership concept, currently no mention of placability at all) | needs link |
| Placability is per-*tile*, not per-column — single-tile writes are real base-game usage (`Incursion`, `Invasive_Dragon`, `Rogue-Dwarf`), so a one-row probe for "which edge is mine" is an assumption, not an invariant | `cube-chaos-scripting/references/targeting-movement-board.md` ("Finding my own edge column") | `cube-chaos-scenario-scripting` (scenario-authored placability), `cube-chaos-audit` (its faction/edge recipe relies on this being understood) | linked |
| Sprite-sheet column count must be read from the sheet's real pixel width, never `ceil(sqrt(n))` — the two diverge silently once a sheet lags its block count | `cube-chaos-sprite-art/SKILL.md` ("Fixed tile sizes", the `real_cols` corollary) | `cube-chaos-scripting` (block insertion order is what triggers the divergence), plus any future tool that reads a sheet | linked |
| A placed cube mirrors horizontally whenever its `DATA:`/`DATARECT:` faction argument isn't `1` | `cube-chaos-sprite-art/SKILL.md:45` | `cube-chaos-scenario-scripting/references/battle-and-terrain-maps.md` (the skill that actually authors those faction args) | needs link |
| Every `Ability:`/`WorldAbility:` needs a paired `Text:`/`Description:`, written in the same edit | `cube-chaos-rule-text/SKILL.md` (whole skill; `CLAUDE.md` states the pairing rule itself) | `cube-chaos-scripting/SKILL.md`'s own Text:/Description: section doesn't point back to `cube-chaos-rule-text` despite `cube-chaos-rule-text` pointing to `cube-chaos-scripting` 6+ times | needs link |
| `render_preview_cards.py` and `render_terrain_screenshot.py` share code (`render_terrain_screenshot.py` imports the former and reuses its chroma-key/icon-crop conventions) | `cube-chaos-sprite-art/SKILL.md` (preview-card section) and `cube-chaos-scenario-scripting/scripts/render_terrain_screenshot.py:270` (credits sprite-art in the script's own comments) | Neither skill's *prose* documents the pairing the way the code itself does | needs link |
| `PERK_REWARD: VALUE:` min/max ranges are concrete evidence of what pricing lands a perk in which reward tier | `cube-chaos-scenario-scripting/references/reward-and-economy-scenarios.md:53` | `cube-chaos-balancing` (this is exactly the kind of empirical pricing evidence that skill wants) | needs link |
| A fixed-percentage self-propagating `Both Die (create)` chain is a supercritical branching process — wipes a whole group once `p × (N-1) ≥ 1` | `cube-chaos-scripting/references/death-fusion-reactive.md` (`BeforeACubeDies`/death-context section) | `cube-chaos-balancing` (this is a balance-relevant fact about any % chance self-propagating effect, not just a DSL note) | needs link |
| A mod's own cube/perk name colliding with a base-game name silently resolves to the base-game one (zero errors) | `cube-chaos-mod-setup/SKILL.md` (filename/name-collision section) | `cube-chaos-audit`'s own checklist has no row for this despite it being a real, already-documented, silently-shipped-content-shaped incident | needs checklist row (not just a pointer) |
| A class/species's base perk and its reward perks split across files can break `BelongsTo:` resolution purely from alphabetical load order | `cube-chaos-mod-setup/SKILL.md` (real Voidling incident) | `cube-chaos-audit`'s checklist has no row for this either | needs checklist row |
| Decompiled `CubeImage:`/`CubeImageXY:` compositing geometry — a guaranteed 5px margin (`Perk.CubeDrawX`/`CubeDrawY`) | `cube-chaos-sprite-art/SKILL.md:287-302` | `cube-chaos-scripting` (`CubeImage:` is itself a `PERK:` DSL field — its own reference should at least point at the rendering consequences) | needs link |
| A gravity-fall trigger shape (`AfterACubeMoves` + `CubeInDirectionFromCube South`, "nothing solid below → fall one tile") | `cube-chaos-scenario-scripting/references/battle-and-terrain-maps.md:165` (found via a terrain trap cube) | `cube-chaos-scripting` — this is a general `Ability:`-chain pattern, not terrain-specific | needs link |
| `Burrowed` + `AiPlacementRule: And AiStacking AiDefense` + `Crumble` is the standard shape for a static ground/terrain-feature tile | `cube-chaos-scenario-scripting/references/battle-and-terrain-maps.md:174` | `cube-chaos-scripting` (general CUBE-authoring convention, not exclusively a terrain-mod concern) | needs link |
| `Animation:` TYPE follows the bound ability's shape — `CLOCK` for a periodic `EveryX...` ability (its last frame lands ON the firing instant), `TRIGGER` only for a reactive `After*` one | `cube-chaos-scripting/references/cube-animation.md` ("Picking the TYPE") | `cube-chaos-sprite-art` (which type is picked decides what its README gif can honestly show, and how many frames to draw) | linked |
| An animation frame is a FULL-tile replacement image (`OverrideWithBIfEqualC`), so two animations on one cube must occupy disjoint pixel regions or one silently loses | `cube-chaos-sprite-art/SKILL.md` ("Animated CUBE icons"), grammar in `cube-chaos-scripting/references/cube-animation.md` | `cube-chaos-audit` has no checklist row for a multi-animation cube's region overlap — it fails with no error, only wrong pixels | needs checklist row |
| Looking up per-item companion files by globbing `<Prefix>_<Category>_<Name>_*` silently mis-attributes them when one item's name prefixes another's (`Rocket` vs `Rocket_Silo`) | `cube-chaos-sprite-art/SKILL.md` (README preview-card section, 2026-08-02 incident) | Any future tooling in `cube-chaos-mod-setup`/`cube-chaos-scenario-scripting` that maps files to named items — same trap, not gif-specific | needs link |
| A fixed-offset positional effect needs a `Visual:` placement-preview line per affected tile; own-tile (`0 0`) and random/dynamic destinations deliberately get none | `cube-chaos-scripting/references/authoring-and-inheritance.md` ("`Visual:` — placement-preview markers", required-vs-omit table) | `cube-chaos-orchestrator`'s `content-cube.md` step 1b and `editing-checklist.md` both now run the grep and point here; `cube-chaos-audit` has a Universal checklist row + detection recipe; `.claude/hooks/check-visual-coverage.sh` is the mechanical backstop | linked 2026-08-02 |
| An ability's duplicate-grant behaviour has three distinct engine modes (merge STACKING / discard / new independent instance) selected by `NO_DUPLICATES` + whether it has a `STACKING` param — and a discarded grant silently suppresses its own `WhenYouGainThis` trigger and grant visual | `cube-chaos-scripting/references/authoring-and-inheritance.md` ("Duplicate-grant model: pick it from the ability's SHAPE") | `cube-chaos-balancing` (which model is chosen decides whether a second copy of an effect is worth anything — same question as its perk-stacking value check); `cube-chaos-audit` has two Universal checklist rows | needs link |
| Map nodes are rewritten from an ordinary `PERK:` (`AfterAMapIsGenerated` + `EveryMapNodeWhich`/`MapNodeIsType`/`SetMapNodeScenario`), never by authoring a new `NODEMAP:` — and the first map is generated *before* class selection, so a class/species perk must duplicate the chain into an `ObtainAction:` | `cube-chaos-scenario-scripting/references/nodemap-generation.md` ("Rewriting an already-generated map from a `PERK:`") | `cube-chaos-scripting/references/perk-economy.md` now points here (trigger-timing is a perk fact, not only a scenario one); `cube-chaos-audit` has two Universal checklist rows | linked 2026-08-02 |
| `cube-chaos-orchestrator`'s Step D ground-truth table should list every skill with a `## Research protocol` section | `cube-chaos-orchestrator/SKILL.md` Step D | `cube-chaos-audit`, `cube-chaos-repo-setup`, `cube-chaos-doc-audit` all have one and are now listed | resolved 2026-08-02 |
| A keyword that *modifies another ability* (rather than having its own effect) must be split into a `COMPOUND: ACTION` holding the mechanic plus a never-granted text-only `COMPOUND: ABILITY` twin for `\A` to resolve against — there is no `GenericTrigger`, so one ABILITY compound can't serve users with different triggers | `cube-chaos-scripting/references/authoring-and-inheritance.md` ("A keyword that MODIFIES another ability") | `cube-chaos-rule-text/SKILL.md`'s keyword-header section now points here (the wording is unchanged but the two-compound shape isn't obvious from the prose side) | linked 2026-08-03 |
| A **computed-value** ability grant takes the bare coloured name + "equal to \<expr\>" in prose, never `\A` — `\A` substitutes the literal parameter and would print the wrong number | `cube-chaos-rule-text/SKILL.md` (the three mechanical cautions under "Why `\A` at all") | `cube-chaos-scripting` (`GainAbilityStacking`/`XTimes`-computed grants are exactly where this arises); `cube-chaos-audit` has no row for it yet | linked 2026-08-03 |
| `Difficulty` starts at **0** and tracks battles won, so any stat scaled off it must be floored/offset or the cube is born at 0 hp on battle one | `cube-chaos-balancing/SKILL.md` ("Scaling a stat off `Difficulty`") | `cube-chaos-scripting` (it's a plain DOUBLE production usable in any chain, with no hint of the 0 floor); `cube-chaos-scenario-scripting` owns the `ChangeDifficulty` sites that drive the counter | linked 2026-08-03 |
| Script-written `.c.png` files never fire `regen-preview-cards.sh`/`regen-readme-previews.sh` (both match `Write\|Edit` + `tool_input.file_path`), so a scratchpad-generated sheet needs a manual `render_preview_cards.py` run — the stale card looks like bad slot math | `cube-chaos-sprite-art/SKILL.md` ("Script-written sheets don't trigger the regen hooks") | `cube-chaos-mod-setup` (owns the test/verify loop and README/Preview governance); `cube-chaos-scenario-scripting` (`render_terrain_screenshot.py` is generated the same way) | linked 2026-08-03 |
| A terrain cube whose ability is gated on `IsAllyToCaster`/`Forwards` must be placed at faction `1`/`2`, never `0` — a neutral copy silently degrades to an inert wall (`Staircase`, the engine's stock traversal primitive, is the load-bearing case) | `cube-chaos-scenario-scripting/references/battle-and-terrain-maps.md` ("Give every ability-bearing terrain cube to the faction meant to USE it") | `cube-chaos-scripting/references/targeting-movement-board.md` ("Hardcoding `East`/`West`") now points here — same faction-symmetry mandate, scenario layer; `cube-chaos-audit` still has no checklist row for an inert-neutral terrain cube | linked 2026-08-03, needs checklist row |
| Map-placed cubes fall: a diagonal/floating terrain feature needs a `Burrowed` cube under every tile, and `render_terrain_screenshot.py` does NOT simulate gravity so a collapsing layout looks intact in the PNG | `cube-chaos-scenario-scripting/references/battle-and-terrain-maps.md` ("A diagonal/floating terrain feature needs a `Burrowed` bed") | `cube-chaos-scripting/references/targeting-movement-board.md` (gravity is a general board fact, not terrain-only) now points here; the screenshot's own known-gaps paragraph in the same file now names this gap | linked 2026-08-03 |
| `IsPositionEmpty <P>` already short-circuits on the null check, so it returns `false` off-board and `And PositionExists <P> IsPositionEmpty <P>` is redundant; `PositionExists` alone is only for "is this the map edge" regardless of occupancy | `cube-chaos-scripting/references/targeting-movement-board.md` ("`IsPositionEmpty` already implies `PositionExists`") | `cube-chaos-audit` has no row for the redundant-guard pattern (it's harmless, but it's the shape that makes people think an empty-check is unsafe alone); two live instances remain in `General_Cubes.c.txt` (`Bomber`, `Take_Off`) | needs checklist row |
| `AddCubeToInventory`/`AddCubeToDeck` take a full `CUBE` production, so a starter/grant can be randomized from a custom pool via `APRandomCubeInLibraryWhich` + a name or `HasCategory` filter — `AP` is the campaign-**seeded** variant and the only one valid in meta-game context | `cube-chaos-scripting/references/creation-and-copying.md` ("Randomizing a grant from a custom pool") | `cube-chaos-scripting/SKILL.md`'s starting-cube baseline note now points here; `cube-chaos-balancing` (a randomized starter's power has to be judged across the whole pool, not one cube) | linked 2026-08-03 |
| `-N Animated Cubes` in `Log.txt` counts cubes with a **declared** `Animation:` line, not successfully-loaded frame files — so it cannot detect a missing/misnamed PNG (that's the separate `Failed to find animation file:` ERROR), and a static recount won't reconcile if a `Loading_Order.txt` package lives outside `GameData/` | `cube-chaos-scripting/references/cube-animation.md` ("The count is a DECLARATION count") | `cube-chaos-mod-setup` (owns the launch-and-check-`Log.txt` loop, where this count is read); `cube-chaos-sprite-art` (owns the frame-file naming/placement that the other check covers) | needs link |
| Holding `ChargeEveryX` **and** `FleeEveryX` at once is legal and yields net drift = difference of the two rates (they're independent timers over the same one-line compound); the old "it jitters" claim is only true at equal periods | `cube-chaos-scripting/references/targeting-movement-board.md` ("Holding BOTH at once") | `cube-chaos-balancing` (a net-drift melee cube's effective aggression differs from a plain charger's); zero base-game precedent, so `cube-chaos-audit` should treat it as unverified-in-play, not as a bug | needs link |
| `HasCategory Test <Tag>` is case-sensitive (`ArrayList.contains` → `String.equals`, no normalisation); a `TYPE`/filter casing mismatch silently matches nothing with no error — the base game's own `No_Class` has this bug (`ATTACKER` vs the universal `TYPE Attacker`) and grants 2 cubes while promising 3 | `cube-chaos-scripting/references/creation-and-copying.md` ("Randomizing a grant from a custom pool") | `cube-chaos-audit` has no row for TYPE-vs-HasCategory casing drift — it's a paired-grep check, exactly the shape of its other detection recipes | needs checklist row |
| A perk granting a cube from a *filtered random pool* has no literal `CubeConstant` token, so its tooltip/preview referenced-cube icons come out empty unless explicit `ReferenceCube:` lines are added | `cube-chaos-sprite-art/SKILL.md` (README preview-card section, two-source lookup) and `cube-chaos-scripting/references/creation-and-copying.md` | `cube-chaos-scripting/references/creation-and-copying.md` now points here from the random-pool section; real instances: DJ's class perk, Unholy's `Lichdom` | linked 2026-08-03 |
| A death/destruction visual must be an `HP` animation, never a `TRIGGER` bound to `AfterThisDies`/`BeforeThisDies` (the cube is gone before the flourish could draw) — 0 of 53 base-game `TRIGGER` animations bind to a death trigger, and `Container` has an `AfterThisDies` yet still uses `Animation: Crumble HP` | `cube-chaos-scripting/references/cube-animation.md` ("It should animate when it's destroyed") | Now linked both ways with `cube-chaos-sprite-art` (owns the `HP`-frame-0-is-the-battlefield-look rule) and carved out inline from the same file's "Picking the TYPE" section, which otherwise sends `After*` triggers to `TRIGGER`; `cube-chaos-audit` still has no row for a death-animation bound to the wrong TYPE | linked 2026-08-04, needs checklist row |
| `EQUAL <Amount>` on an `HP` animation **always** wastes its last frame, at any max hp (thresholds sum to exactly 1.0; ratio hits 1.0 only at 0 hp = dead) — so all 77 base-game `HP` animations really animate in `Amount-1` stages; a low-max-hp cube additionally loses more, and the fix is an explicit list summing to <1.0 | `cube-chaos-scripting/references/cube-animation.md` ("`EQUAL <Amount>` on an `HP` animation ALWAYS wastes its last frame") | `cube-chaos-sprite-art` (an unreachable frame is wasted drawing effort — worth knowing before painting the strip, not after); `cube-chaos-balancing` (a max-hp change silently re-maps which frames are reachable) | needs link |
| The no-counter perk-stacking idiom extends from "the next untouched CUBE" to "the next untouched POSITION" (scan until a tile the effect itself invalidates) — and that shape forces a manual `Until` scan, because the engine has **no** position iterator (`ARandomPositionWhich` is the only `BOOLEAN`-filtered POSITION production) | `cube-chaos-scripting/references/perk-economy.md` ("The same no-counter stacking idiom extends... to the next untouched POSITION") | `cube-chaos-scripting/references/targeting-movement-board.md` owns the `SetVariable`+`Until`+`ChangeVariable` scan skeleton this reuses; real instance: Broker's `Downtown` | needs link |
| The first-map `AfterAMapIsGenerated` trap is **not** map-node-rewriting-specific — it applies to any payload on a class/species perk, incl. a bare `ReadAScenario <X>`, and the `COMPOUND: ACTION` rule applies even to a one-token payload | `cube-chaos-scenario-scripting/references/nodemap-generation.md` ("The first map is generated BEFORE class/species selection") | Already linked both ways (`cube-chaos-scripting/references/perk-economy.md`'s Blight/Boon trigger bullet ↔ that section); the 2026-08-04 Broker Forge case is what generalised it past map-node rewriting | linked 2026-08-04 |
| A real `<Prefix>_UpgradePerks.c.png` is now honoured by `render_preview_cards.py` (before 2026-08-04 it always walked `IsUpgradeFrom:` back to the base perk's icon, so a drawn upgrade sheet rendered a correct in-game icon but a wrong preview card) | `cube-chaos-sprite-art/SKILL.md` ("`render_preview_cards.py` honours a real upgrade sheet as of 2026-08-04") | `cube-chaos-mod-setup` (owns README/Preview governance, where the wrong-but-plausible card would surface); first instance: Broker's `Downtown` | needs link |
