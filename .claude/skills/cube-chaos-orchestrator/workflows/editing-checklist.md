# Editing-only checklist

Rules that only apply when changing something that already exists, on top of whatever the matching content-type workflow (`content-cube.md`, `content-perk-family.md`, `content-class-species.md`) already covers for fresh creation. Read this *before* making the edit, not after — several of these are about not treating the edit as "just the DSL change."

- **A design-changing edit still goes through the orchestrator's Step C preview-and-approve gate first** — if the edit alters an `Ability:`/`WorldAbility:`/`SpecialAction:` chain or a balance number (mana/hp/IDENT/`Value:`/`BalanceCap:`), print the revised theoretical spec (with the `Text:`/`Description:` re-derived from the *new* chain) and get the user's explicit OK before touching the file. A pure reword or pure sprite-pixel edit is exempt (nothing about the design is being decided), same as those are exempt from the launch loop.
- **An `Ability:`/`WorldAbility:`/`SpecialAction:` change updates its paired `Text:`/`Description:` in the same edit.** Not a follow-up, not optional, even for a one-word change (a direction changing from `North` to `East` means the word "above" in the prose has to become "to the east," not just the DSL token). This is `cube-chaos-scripting`'s Text:/Description: requirement plus `cube-chaos-rule-text`'s "rule text is never edited independently" principle, stated together because in practice they get missed together.
- **A changed positional/directional effect means re-running the `Visual:` grep, not just editing the ability logic.** Run `grep -nE 'PositionInDirectionFrom|CubeInDirectionFromCube|TopPositionAboveCube' <file>` over the edited block and re-derive every marker from `cube-chaos-scripting/references/authoring-and-inheritance.md`'s required-vs-omit table. **A *missing* marker is as much a bug as a stale one** — an earlier version of this line only warned about markers that had gone out of date, which is why cubes that never had one (`Hell_Portal` and 11 others, found 2026-08-02) were never caught by it. Verify by count: `Visual:` lines must equal distinct affected offsets.
- **A perk-stacking behavior change means re-checking whether the "additional copies of this perk..." clarifying sentence is still warranted** — per `cube-chaos-rule-text`, that sentence belongs only when the re-trigger is a genuinely non-obvious/separate mechanism (like a repeated internal scan), not when stacking is just the natural consequence of a "first cube/target matching X" search. Don't add it reflexively just because a perk stacks; don't leave a stale one in either if a rewrite changed the mechanism.
- **A sprite edit stays scoped to exactly the one tile being changed** — its full bounding box, including that tile's own border pixels — never a wider redraw of a shared sheet, even to "fix" something that looks inconsistent elsewhere. If the border itself needs regenerating for that tile, use the from-scratch generators in `cube-chaos-sprite-art`'s border-pattern library (position-based), never color-matching against another tile's pixels — that's what caused the DJ-headphones-bleeding-into-other-icons incident this skill set exists partly to prevent.
- **If the tile being edited is a class/species's own base-perk icon (tile 0 of its `PERK:`/species file) and `GameData/<Mod>/Image.png` already exists, regenerate `Image.png` from the new pixels too** — it's a derived copy of that exact tile (crop, strip the 1px magenta guide border, nearest-neighbor upscale; see `cube-chaos-mod-setup`'s `references/workshop-publishing.md` for the exact recipe), so an icon redraw that doesn't also redo `Image.png` leaves the Workshop thumbnail silently showing the old art. If the mod has no `Image.png` yet, this doesn't apply — same "only if it already exists" gate as the README/Preview rule below.
- **A grid-resizing sprite change (adding enough new content to need a bigger grid) relocates existing tiles' exact pixels, never redraws them** — extract each tile's current pixel box, paste into its new grid position unchanged, then add the new tiles.
- **Adding a new `CUBE:`/`PERK:` block anywhere but the end of its `.txt` file shifts every later block's sprite slot by one, even with no grid resize** — slot assignment is pure file order, so the new block's position in the file must match the grid slot its art is actually drawn into. Simplest: always append new blocks at the end of the file (the last free slot), don't insert next to a "thematically related" sibling partway through. See `cube-chaos-sprite-art`'s corresponding gotcha for the real incident this came from.
- **Expect the Claude Code sandbox to block the script that does this, even though it's the correct/documented approach.** A Python script that rewrites a whole sprite sheet file (necessary to resize its canvas) reads as "full-file overwrite" to the auto-mode permission classifier, which denies it as irreversible local destruction regardless of whether the logic only relocates existing pixels byte-for-byte with no redrawing. Don't try to work around the block (chunked writes, a different tool) — stop and use `AskUserQuestion` to get explicit confirmation for that specific script, then re-run the identical command. Real precedent: General mod's `Revenge` perk icon required growing `General_Perks.c.png` from 2x2 to 3x3; the relocate-and-add script was denied on first attempt and succeeded unmodified after the user confirmed.
- **Both mods in this repo now keep `IsUpgradeFrom:` upgrade perks in their own sprite-less `<ModPrefix>_UpgradePerks.c.txt`, separate from the regular perks file** (matching base-game convention — see `cube-chaos-sprite-art`'s upgrade-perk section), specifically so a regular perks file's sheet never has to account for upgrade-shaped blank slots at all. Adding a new regular perk is just "count the real `PERK:` blocks in the regular file"; adding a new upgrade perk needs no sprite step at all (append to `_UpgradePerks.c.txt`, done). If you ever encounter a `.c.txt` file that still mixes the two (a leftover from before this convention, or a fresh mistake), the old rule still applies as a fallback: **count every `PERK:` block in file order, including `IsUpgradeFrom:` ones**, when recomputing slot indices — an upgrade's slot is conventionally left blank but still occupies a position in the sequence, and skipping it in the count shifts every later perk one slot off (`WARNING: Perk <name> ... with empty Image`, no load error). Better to split the file at that point than keep counting carefully forever — see `cube-chaos-sprite-art`'s upgrade-perk section for the full incident writeup and reasoning.
- **Adding/removing a `Value:` line changes what economy the perk participates in, not just a cosmetic number** — check `cube-chaos-scripting`'s Perk economy section before touching one on an existing perk. A plain class/species reward perk must have none at all; every other category (Curse/Blight/Boon/Terrain/Consumable/Golden/Neutral/CubeUpgrade) is expected to have one. Don't add one "just in case" to a reward perk — it makes the perk sellable in shops via `Sell_One_Positive`'s `IsLarger ValueOfPerk Target 0` filter, a real behavior change, not a no-op.
- **Renaming a `PERK:` breaks any self-name check silently** (`AmountOfPerksInInventoryWhich IsSameString NameOfPerk Test StringConstant <OldName>` keeps reading zero forever, no error) — if the perk being edited does this kind of self-reference anywhere, update the literal string too.
- **Always finish with a fresh test-launch and `Log.txt` check**, even for an edit that "should be safe" — a change that only touches prose or only touches a sprite can still desync the parser or collide with a filename, and the previous launch's clean log doesn't cover new edits made since.
- **If this mod already has a `GameData/<Mod>/README.md`, then regenerate its preview cards** (`python3 .claude/skills/cube-chaos-sprite-art/scripts/render_preview_cards.py`) — an edited `Description:`/`Value:`/icon that isn't reflected in `GameData/<Mod>/Preview/` is stale documentation, and it's easy to forget once the DSL/log-check part of the edit is already "done." If the mod has no README yet, skip this — see `cube-chaos-mod-setup`'s README governance section; don't create one just because this edit happens to be the kind of change a README would normally need refreshing for.
